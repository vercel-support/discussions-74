# Headers Redirect Example

Browsing to `/abc` when `x-vercel-ip-country` returns `GB` will redirect to `/`.

Browsing to `/xyz` when visiting from a device with a UA containing `Mozilla` will redirect to `/`.
